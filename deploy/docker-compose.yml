services:
  harness:
    build: .
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./sealed:/app/sealed
    # localhost-only publish; do not expose sealed store on LAN
    command: ["sealed-eval", "serve", "--host", "0.0.0.0", "--port", "8787"]

  subject-demo:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    command:
      [
        "uvicorn",
        "app:app",
        "--app-dir",
        "subject-demo",
        "--host",
        "0.0.0.0",
        "--port",
        "8080",
      ]

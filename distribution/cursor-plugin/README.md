# SEALed-eval (Cursor)

Eval-operator profile. Point MCP/HTTP at local harness:

```
sealed-eval serve --port 8787
```

Read `../../agents/EVAL_AGENT.md` and `../../agents/SETUP_AGENT.md`.
Coder sessions: install nothing that can `seal_corpus`.

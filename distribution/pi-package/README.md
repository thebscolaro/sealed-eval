# SEALed-eval (Pi)

Pi package stub. Call harness via HTTP/CLI (no MCP required):

```
sealed-eval capabilities
sealed-eval propose … 
sealed-eval seal …
sealed-eval grade …
```

Role: `../../agents/EVAL_AGENT.md`
